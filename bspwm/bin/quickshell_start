#!/bin/sh
# Quickshell from nix looks for GL drivers under /run/opengl-driver which does not exist outside NixOS.
export QT_QUICK_BACKEND=software

pkill -xf 'quickshell --daemonize --no-duplicate'

quickshell --daemonize --no-duplicate
