#!/bin/sh

xrdb -load -I"$HOME/.config/xrdb" ~/.config/xrdb/Xresources.xdefaults
# xdotool key Super_L+F5

# update st
pkill -USR1 -x st

pkill polybar # it's launched by bspwm again

# update dunstrc
. ~/.config/dunst/reload_dunst
. ~/.config/bspwm/bspwmrc
